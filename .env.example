# LLM Provider: anthropic | openai | nebius
LLM_PROVIDER=anthropic

# Anthropic API Key (if using anthropic provider)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (if using openai provider)
OPENAI_API_KEY=your_openai_api_key_here

# Nebius serverless vLLM (if using nebius provider)
NEBIUS_API_KEY=your_nebius_api_key_here
NEBIUS_BASE_URL=https://api.studio.nebius.com/v1/
# Any model available on your Nebius endpoint, e.g.:
#   meta-llama/Meta-Llama-3.1-70B-Instruct-fast
#   Qwen/Qwen2.5-72B-Instruct
#   mistralai/Mistral-Nemo-Instruct-2407

# --- Self-hosted GPU job mode (Dockerfile.gpu + entrypoint.sh) ---
# Runs a local vLLM server on the job's own GPU instead of calling a
# hosted endpoint. NEBIUS_API_KEY/NEBIUS_BASE_URL are set automatically
# by entrypoint.sh in this mode - you only need to set MODEL (and
# optionally the vLLM tuning vars below).
#   MODEL=Qwen/Qwen2.5-7B-Instruct        # must fit the job's GPU preset
#   VLLM_PORT=8000
#   VLLM_GPU_MEM_UTIL=0.90
#   VLLM_STARTUP_TIMEOUT=600               # seconds to wait for model load
#   VLLM_EXTRA_ARGS=--max-model-len 8192   # passed through to vLLM
#   HUGGING_FACE_HUB_TOKEN=your_hf_token   # required for gated models

# Generation parameters
N_PATIENTS=5
OUTPUT_DIR=data/output
MODEL=claude-sonnet-4-6

# Diagnostic/procedure codes driving admission generation. Any coding
# standard with a JSON file under code_systems/ can be used via
# DIAGNOSTIC_CODE_SYSTEM / PROCEDURE_CODE_SYSTEM (default: icd10 / opcs4).
# See src/codes/loader.py for the file format to add another standard.
DIAGNOSTIC_CODES=I21.0,J18.1
DIAGNOSTIC_CODE_SYSTEM=icd10
PROCEDURE_CODES=
PROCEDURE_CODE_SYSTEM=opcs4

# Optional directory of extra code-system JSON files, e.g. a mounted
# volume in a serverless job - loaded in addition to (and able to
# override, by reusing a key) the built-in ones under code_systems/.
# Leave unset to use only the built-in standards.
EXTRA_CODE_SYSTEMS_DIR=

# Backward-pass check: after generation, verify each driving code is
# actually reflected in the admission/notes, and (if not) apply a targeted
# corrective regeneration. Correction is on by default (1 attempt); set to
# 0 to disable and only log misses.
MAX_CORRECTION_ATTEMPTS=1

# Code research: for codes with no curated dictionary entry, look them up
# via Google Custom Search before generation so the LLM has real clinical
# grounding instead of guessing from the bare code alone. Off by default -
# adds a search + LLM call per unique uncurated code (cached for the run).
# Get an API key at https://console.cloud.google.com (enable "Custom Search
# API") and a CSE ID at https://programmablesearchengine.google.com/
# (configure it to search the entire web).
RESEARCH_UNKNOWN_CODES=false
GOOGLE_SEARCH_API_KEY=
GOOGLE_SEARCH_CSE_ID=

# Quality evaluation (--evaluate-notes): scores generated notes on
# readability plus LLM-judged fluency/groundedness/relevance/factuality/
# redundancy. Off by default (adds 5 LLM calls per note).
EVALUATE_NOTES=false

# Judge model for --evaluate-notes' LLM-judged metrics. Deliberately kept
# distinct from (and larger than) MODEL so a model doesn't grade its own
# output. Leave unset to use a per-provider default
# (see src.processing.default_judge_model).
JUDGE_MODEL=

# Self-consistency: generate each note this many times and keep the
# variant that reflects the most driving diagnostic/procedure codes, using
# cross-generation code-reflection agreement as a no-ground-truth proxy for
# generation stability. Adds (N-1)x LLM calls per note. 1 = disabled.
SELF_CONSISTENCY_N=1

# Corrective action pass (only with --evaluate-notes): notes scoring below
# QUALITY_THRESHOLD on fluency/groundedness/relevance/factuality are
# revised once and re-scored. Set CORRECT_LOW_QUALITY=false to disable and
# only measure, not act on, low-scoring notes.
QUALITY_THRESHOLD=0.7
CORRECT_LOW_QUALITY=true

# Journey/generation tuning
N_EVENTS_PER_PATIENT=8
TYPO_RATE=0.02
MAX_LLM_ATTEMPTS=5
MIN_LOS_DAYS=1
MAX_LOS_DAYS=14
